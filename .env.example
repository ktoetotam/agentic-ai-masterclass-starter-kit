# Copy to .env on your own computer. Never commit .env or share real secrets.
# These sample values are public-safe; no API account is needed for local work.
TEAM_NAME=your-team
PORT=4173
# Add provider-specific variables only for integrations your team has chosen.
# Put actual credentials in .env, never in this example or in public/.
# Optional deployment only: add CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_API_TOKEN
# to .env after the facilitator assigns your workshop target and scoped token.
